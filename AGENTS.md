## 基本约束
### 核心行为准则
**先读再改** — 修改代码前必须先读文件，不要盲目编辑
**最小改动** — 只改需求要求的，不加额外功能，不提前抽象
**风险操作需确认** — 删除文件、force push、删表等操作先问用户
**没有允许的情况下禁止使用git命令** — 不允许使用git
**没有主动要求编译文件测试的时候不要编译文件测试**
**只做用户指定的功能，如果检测到可能需要额外做其功能就询问用户是否需要扩展功能**
**没有明确要求写单元测试的时候不要写单元测试**

**如果没有必要，不要读取target文件夹内容**

**禁止读取下面文件夹的内容，以免浪费token**

- .idea
- .git
- .mvn

### 文件与编码规范
- 所有由 Agent 新建或修改的源码、配置、Markdown 文档必须保持 UTF-8 编码。
- 代码文件在本 Windows 项目中默认使用 CRLF 行尾；如果既有文件明显采用其他行尾，优先保持原文件风格。
- 读取或写入中文内容时必须显式按 UTF-8 处理，避免出现乱码后继续编辑。

### 代码落地质量红线
- 写代码必须考虑性能，不允许在循环中调用 DB 或 Mapper 方法。
- 能批量查询、批量更新、预加载 Map、分组后集合化处理的，不要逐条查库或逐条更新。
- 大结果集必须分页、分批或按项目既有模式流式处理，不能无界加载到内存。
- 如果确实无法避免循环处理，完成报告中必须说明原因、数据规模边界和风险。
- 在代码块中插入代码的时候注意不要将原有代码的代码和它的注释隔离开
- 写的代码合理加注释,注意中文注释不要乱码
- 新建实体类，字段名称要使用多行注释

### Java 类署名规范
- 新增类文件时，按既有项目风格补充类级 Javadoc。
- 作者统一写 yj，日期写创建类的日期

示例：
```java
/**
 * ...
 *
 * @author yj
 * @date yyyy-MM-dd HH:mm:ss
 */
```

## 协助ai理解项目，节省token

### 知识图谱（graphify）
项目知识图谱在 `graphify-out/` 目录，包含社区结构和跨文件关系。

**必守规则：**
1. 没有给出功能模块路径的时候先读 `graphify-out/GRAPH_REPORT.md`这是你的代码地图，如果明确给出了功能入口地址就不用看知识图谱，直接定位位置
2. 做项目需求的时候如果该项目没有知识图谱，先生成该项目的知识图谱，再基于知识图谱来处理需求



### 本地文档
- `docs/db_schema_graph.md` — 数据库表结构图（查表结构优先用这个，再考虑 MySQL MCP）
- `docs/superpowers/` — 历史设计文档、分析产物、SQL
    - `docs/superpowers/specs/` — 技术设计文档
    - `docs/superpowers/plans/` — 实施计划
    - `docs/superpowers/sql/` — SQL 脚本
- 在分析新需求前，**先检查 `docs/superpowers/` 是否有可复用的文档**

